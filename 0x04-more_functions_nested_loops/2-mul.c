#include "main.h"

/**
 * mul - function for conducting multiplication between two numbers
 * @a: pass parameters
 * @b: pass parameters
 * Return: returns 0
 */
int mul(int a, int b)
{
return (a * b);
}
